# ROS2_Service_Client
This project controls the TurtleSim turtle using a ROS 2 Python node. The node reads the turtle’s position and moves it around the screen. When the turtle crosses X = 5.5, the pen color changes between red and green. If the turtle goes near the edges, it turns to stay inside the area.
